     1→# InvoiceFlow — Agent Worklog
     2→
     3→Shared handover document. Every agent MUST read this file before working and append a section (never overwrite) when finished.
     4→
     5→---
     6→
     7→Task ID: 0
     8→Agent: Z.ai Code (main orchestrator)
     9→Task: Project inspection, baseline setup, canonical spec creation.
    10→
    11→Work Log:
    12→- Inspected repo: Next.js 16 (App Router) + TypeScript strict + Tailwind 4 + shadcn/ui (full set) + Prisma/SQLite + Zustand + Zod + RHF + recharts + sonner already installed.
    13→- Dev server already running on port 3000 (logs: /home/z/my-project/dev.log). DB: file:/home/z/my-project/db/custom.db
    14→- Installed: dexie, dexie-react-hooks, jspdf, jspdf-autotable.
    15→- Created docs/_CANON.md — the canonical architecture/data-model/sync-protocol spec. ALL agents must derive their docs/code from it for consistency.
    16→- Plan: docs via parallel agents (D1..D8), Supabase SQL agent (E1), Electron scaffold agent (E2), core app built by orchestrator, PDF/Reports/Settings via agents (F1..F3), then lint, browser E2E verification, cron handoff.
    17→
    18→Stage Summary:
    19→- Baseline ready. Canonical spec at docs/_CANON.md is the source of truth for all modules.
    20→- Constraint notes: sandbox preview can only serve the `/` route → the app is an offline-first SPA with hash-based internal navigation; API routes are used (no server actions); tests are described in docs (35/36) but not bundled in the sandbox per environment rules.
    21→
    22→---
    23→Task ID: 2-g
    24→Agent: D7-docs-writer
    25→Task: docs 35-41 (testing/deploy/env/backup/perf/roadmap)
    26→Work Log:
    27→- Read worklog.md and docs/_CANON.md completely (all 266 lines) before writing; derived every file from CANON §2–§19 (sandbox mapping, §4 money math, §7 Dexie indexes, §9 sync protocol, §16 security, §17 PWA/Electron, §19.3/§19.5 assumptions).
    28→- Wrote docs/35-TESTING.md: full pyramid (unit domain/integration fake-indexeddb+temp SQLite/component RTL/e2e Playwright web+Electron), edge-case contract tables (odd-paise CGST/SGST split per §4 formula, tax-inclusive extraction, round-off ±, FY boundaries, in-words CANON example), test builders, CI GitHub Actions, coverage gates (domain 95%+), Supabase mocking, example vitest + playwright configs, sample computeDocumentTotals test (grandTotalRaw 235999 → roundOff +1 → 236000), explicit CANON §19.5 no-bundled-tests statement.
    29→- Wrote docs/36-OFFLINE-TESTING.md: setOffline/blockServer interception, SW update (invoiceflow-v1→v2), persistence across reloads, 13-step outbox→online drain walkthrough, two-context conflict recipe, clock tests (overdue, Mar 31/Apr 1 FY boundary, lazy EXPIRED), Electron offline launch via _electron, 16-row scenario×steps×expected checklist.
    30→- Wrote docs/37-DEPLOYMENT.md: Vercel (env vars, build, CSP/HSTS headers, SW prod-only), Supabase (migrations 0001→0003 order, RLS verification gates, storage buckets, PITR), electron-builder matrix (Win NSIS, macOS DMG signed+notarized, Linux AppImage/deb), code signing, semver + GH Actions release flow with YAML, deployment architecture mermaid, rollback, health checks.
    31→- Wrote docs/38-ENVIRONMENT.md: every env var table (DATABASE_URL, NEXT_PUBLIC_*, SUPABASE_* with service-role warning box, SUPABASE_JWT_SECRET + NEXTAUTH_SECRET equivalence note, ELECTRON_START_URL, 3 feature flags), zod env validation, .env.example block, dev/preview/prod/desktop matrix, 90-day rotation policy.
    32→- Wrote docs/39-BACKUP-RESTORE.md: JSON export schema {meta:{app,version,exported_at,device_id},tables:{17 CANON §7 tables}}, Zod import validation, merge policy (keep newer version/updated_at, never auto-delete, sequences = max), scheduled reminder, Supabase PITR + logical dumps, desktop userData pointer, restore-flow mermaid, corrupted-DB DR runbook, acceptance criteria.
    33→- Wrote docs/40-PERFORMANCE.md: budgets (TTI<2s, list<100ms, PDF<1s/50 items, totals<5ms/100 items, pull 500<3s, JS≤300KB gz), verbatim CANON §7 Dexie index list with query rules, useLiveQuery granularity, 500-row virtualization threshold, 10/page pagination, memoized aggregations, lazy jsPDF/recharts dynamic imports, logo ≤1MB + downscale, SW cache strategy, bulkPut 25/batch in one pull transaction, web-vitals monitoring.
    34→- Wrote docs/41-FUTURE-ROADMAP.md: v1.1 (team members UI, email, WhatsApp Cloud API, payment links), v1.2 (Razorpay/Stripe, subscription billing, expenses, purchases), v2.0 (multi-company, multi-currency, inventory, e-invoice IRN note, e-way bill, TDS, mobile via same core) — each with rationale, docs/24–28 pointer, S/M/L effort, CANON §19.3/§19.7 alignment table, timeline mermaid gantt.
    35→- All 7 files use the standard skeleton (Purpose/Scope/Business requirements/Technical design/Data models/Offline/Online/Security/Error-handling/Acceptance criteria/References) with production-quality English and no placeholders.
    36→- Cross-reference alignment pass (post concurrent-appends): docs/41 roadmap pointers updated to the actual extension-doc titles landed by 2-e (WhatsApp→docs/24-WHATSAPP-SHARING, email→docs/25-EMAIL, subscription→docs/26-SUBSCRIPTION, payments/links→docs/27-PAYMENT-INTEGRATION; expenses/purchases/multi-company/currency/inventory→docs/28 design slot); docs/36 drain walkthrough totals restated as ₹2,360.00 (raw 235999 + 1 round-off) matching docs/35 §4.3 exactly.
    37→
    38→Stage Summary:
    39→- Files written: docs/35-TESTING.md, docs/36-OFFLINE-TESTING.md, docs/37-DEPLOYMENT.md, docs/38-ENVIRONMENT.md, docs/39-BACKUP-RESTORE.md, docs/40-PERFORMANCE.md, docs/41-FUTURE-ROADMAP.md (7/7 complete; worklog appended, nothing overwritten).
    40→
    41→---
    42→
    43→Task ID: 2-d
    44→Agent: D4-docs-writer
    45→Task: docs 14-19
    46→Work Log:
    47→- Read worklog.md and docs/_CANON.md in full; treated _CANON.md as the single source of truth (§1 pipeline, §3 metadata, §6 numbering, §7 Dexie schema, §8 cloud, §9 sync protocol, §10 conflict matrix, §11 guest/claim, §13 PDF, §14 API, §16 security, §17 PWA, §19 limitations).
    48→- Wrote docs/14-PDF-GENERATION.md: jsPDF + jspdf-autotable rationale table, UnifiedDocumentModel (snapshots-in, domain-engine totals only), full A4 zone/layout spec, multi-page behavior, offline generation flowchart, Electron printToPDF/save IPC contract, Rs.-vs-₹ font limitation, error matrix, acceptance criteria.
    49→- Wrote docs/15-OFFLINE-FIRST.md: golden-rule pipeline as normative mermaid flowchart (create-invoice trace incl. conflict/validation/backoff branches), IndexedDB source-of-truth table, outbox pattern summary, network detection (navigator.onLine + /api/health heartbeat), guest mode + claim path, PWA sw.js caching rules, offline capabilities matrix, browser limitations & mitigations.
    50→- Wrote docs/16-INDEXEDDB-DATABASE.md: verbatim CANON §7 v1 schema code block (17 tables + index strings) plus reference table, why-Dexie rationale, metadata conventions, one-transaction finalize example (invoice+sequence+audit+outbox), v2 migration example code, storage estimates & eviction mitigations (JSON backup), DevTools debugging tips, local erDiagram, repository "API contract" surface.
    51→- Wrote docs/17-SYNC-ENGINE.md: CANON §9 push/pull JSON reproduced verbatim + all 7 server rules; engine lifecycle (5 triggers, mutex guards, push-then-pull, reclaim of in_flight); op statuses stateDiagram; ProcessedOp idempotency; ChangeLog.seq cursors (push_cursor reserved/unused in v1); retry schedule table (2s→256s, cap 10 min, 8 attempts → failed, manual retry resets); auth-expiry & schema-version guard; batch rules (25 push / 500 pull); two-devices+server sequenceDiagram; outbox lifecycle flowchart; "no silent data loss" guarantee matrix.
    52→- Wrote docs/18-CONFLICT-RESOLUTION.md: CANON §10 matrix reproduced; 3-way merge pseudocode (customer/product/draft docs) with an explicitly justified local-only unindexed sync_operations.base_json deviation (merge-base snapshot; never on the wire, no index change); never-auto-merge list (finalized docs, payments, tombstones, numbers); conflict UI spec (side-by-side diff, Keep mine / Keep server's / Delete); SYNC_CONFLICT audit detail_json; reconnect sequenceDiagram (first wins, second → conflict UI), delete-vs-edit, double-finalize, duplicate-number (§6 server authority), record conflict-lifecycle stateDiagram.
    53→- Wrote docs/19-CLOUD-SYNC.md: Supabase production design (tables mirroring CANON §7 + change_log/processed_ops, RLS SQL excerpts with is_workspace_member/has_role and the 4-role matrix, company-assets/attachments buckets with {workspace_id}/{entity}/{filename}), Edge Functions alternative with trade-offs, dev-cloud adapter (Next API routes + Prisma SQLite, PrismaSyncStore vs SupabaseSyncStore behind a SyncServerStore port), claim flow, deployment architecture flowchart (dev & prod subgraphs), service-keys-server-only security posture.
    54→- Consistency pass: all numeric contracts pinned to CANON (backoff 2^n×2s cap 10 min / 8 attempts; batch 25; pull limit 500; prune 7 days; roles VIEWER read-only, MEMBER no delete, ADMIN no workspace delete; number format {prefix}/{FY}/{seq4}; DRAFT-xxxxxxxx; Rs. in PDFs); each file carries the full section set (Purpose, Scope, Business requirements, Technical design, Data models, API contracts, Offline behavior, Online behavior, Security considerations, Error-handling rules, Acceptance criteria) and the specified mermaid diagrams.
    55→Stage Summary:
    56→- docs/14-PDF-GENERATION.md (PDF system: jsPDF rationale, UnifiedDocumentModel, A4 layout, offline flow + Electron IPC, limitations)
    57→- docs/15-OFFLINE-FIRST.md (offline-first philosophy, pipeline flowchart, capabilities matrix, network detection, guest mode, PWA, browser limits)
    58→- docs/16-INDEXEDDB-DATABASE.md (Dexie v1 schema verbatim, transactions, migrations, storage/eviction, debugging, local ERD)
    59→- docs/17-SYNC-ENGINE.md (definitive sync spec: §9 contracts, lifecycle, statuses, idempotency, cursors, backoff table, sequence/state/outbox diagrams)
    60→- docs/18-CONFLICT-RESOLUTION.md (conflict matrix, 3-way merge pseudocode, conflict UI, audit, reconnect scenarios, conflict state machine)
    61→- docs/19-CLOUD-SYNC.md (Supabase design + RLS SQL + buckets + Edge Functions, dev-cloud Prisma adapter, SyncServerStore port, deployment diagram)
    62→
    63→---
    64→
    65→Task ID: 2-e
    66→Agent: D5-docs-writer
    67→Task: docs 20-27
    68→Work Log:
    69→- Read worklog.md and docs/_CANON.md in full; derived all content from CANON §3-§17/§19 (money in paise, statuses, outbox protocol, storage buckets, UI states, extension-point list).
    70→- Wrote 20-DASHBOARD.md: 8 KPI cards with exact formulas (Total invoices = all non-deleted except CANCELLED incl. drafts; Drafts/PAID/Unpaid/Overdue per status predicates; Total quotations; Accepted = ACCEPTED+CONVERTED; Outstanding = Σ(grand_total−paid_total) of FINALIZED+PARTIALLY_PAID), supporting metrics (Total revenue = Σ payments; Invoiced = Σ grand_total of FINALIZED+PARTIALLY_PAID+PAID), FY-default period selector, monthly invoiced-vs-collected chart (trailing 6 buckets), recent transactions, sync status + workspace cards, pure-aggregator design, mermaid Dexie→aggregation→cards data flow.
    71→- Wrote 21-REPORTS.md: 5 reports (Sales; GST Summary grouped by month with CGST/SGST/IGST; Outstanding with 0-30/31-60/61-90/90+ aging clamped at due_date-or-invoice_date; Customers by collected revenue; Products by Σqty_milli + Σ line totals), FY-default date ranges, exact CSV spec (UTF-8 BOM, comma, RFC-4180 quoting, CRLF, per-report column dictionaries), client-side pure aggregation over IndexedDB snapshot columns, GST-summary PDF export marked as extension point.
    72→- Wrote 22-STORAGE.md: attachments table (inline blobs ≤2 MB MVP, local-only), logo/signature dataURLs ≤1 MB PNG/JPEG on company_profiles, Supabase buckets company-assets/attachments with {workspace_id}/{entity}/{filename} paths + RLS policy sketch, quota guidance (50 MB soft budget, storage.estimate), orphan sweep + future cloud GC, signed-upload API sketch, lifecycle mermaid.
    73→- Wrote 23-NOTIFICATIONS.md: sonner toast catalog with coalescing/dedupe/duration rules via notify.ts typed event bus, sync pill state machine (Synced/Pending N/Syncing/Offline/Error/Reauth) with stateDiagram, overdue badges, browser-push + Electron-desktop extension points, future notification_prefs design.
    74→- Wrote 24-WHATSAPP-SHARING.md: banner-marked extension point; MVP = PDF download + Web Share API; future wa.me deep-link tier + WhatsApp Business Cloud API server-side design (why server+public URL is mandatory), share_outbox queue with engine backoff + idempotency, template compliance (opt-in, 24h window, {{1..5}} mapping), registration/send payload examples, webhook signature verification, sequence diagram.
    75→- Wrote 25-EMAIL.md: banner-marked extension point; Edge Function + Resend/SES adapter, client-rendered PDF uploaded to storage and attached server-side, templates (invoice_sent/payment_reminder/quotation_sent/trial_ending) with merge-field tables, email_log table + statuses, email ops join the outbox (entity enum v2 bump, same retry/backoff), SPF/DKIM/DMARC note, no SMTP creds client-side, render pseudocode.
    76→- Wrote 26-SUBSCRIPTION.md: banner-marked extension point; Free/Pro/Business catalog, plan_limits/subscriptions/usage_counters SQL sketch, assertEntitlement in push pipeline returning per-op rejected/plan_limit (reuses CANON push contract), entitlements cache TTL pattern, Stripe/Razorpay adapter + webhook lifecycle mermaid, 14-day no-card trial with grace, BILLING_ENFORCED=false dev flag.
    77→- Wrote 27-PAYMENT-INTEGRATION.md: per-feature status header; IMPLEMENTED manual recording (eligibility, exact allocation/recalc pseudocode, overpayment-as-advance, two-op enqueue + audit), future Razorpay/Stripe collection (server-side order creation from server-computed balance, signed webhooks → same trusted allocation, 3-layer idempotency), sequence diagram, UPI intent links (manual reconciliation), refunds design (refunds table, refunded_total_paise, effective_paid recalc, cancel invariant preserved).
    78→- Validation pass: all 8 files contain the 11 required sections; 157-189 lines each; mermaid blocks present in 20/22/23/24/26/27; cross-references use canonical doc numbers from CANON.
    79→Stage Summary:
    80→- docs/20-DASHBOARD.md — KPI formulas, period/chart/cards, data-flow diagram (170 lines)
    81→- docs/21-REPORTS.md — 5 reports, aging buckets, CSV spec + column dictionaries (164 lines)
    82→- docs/22-STORAGE.md — local/cloud attachment design, RLS sketch, quotas, cleanup (166 lines)
    83→- docs/23-NOTIFICATIONS.md — toast catalog, sync pill machine, overdue badge, push/desktop extension points (162 lines)
    84→- docs/24-WHATSAPP-SHARING.md — MVP sharing + WhatsApp Cloud API future design, share queue, sequence diagram (189 lines)
    85→- docs/25-EMAIL.md — Edge-Function email pipeline, templates, email_log, outbox integration (157 lines)
    86→- docs/26-SUBSCRIPTION.md — plans, entitlements, billing tables/hooks, gating, trials (178 lines)
    87→- docs/27-PAYMENT-INTEGRATION.md — manual payments (implemented) vs gateway/UPI/refunds (extension points), sequence diagram (157 lines)
    88→
    89→---
    90→
    91→Task ID: 2-b
    92→Agent: D2-docs-writer
    93→Task: docs 05-08
    94→Work Log:
    95→- Read worklog.md and docs/_CANON.md in full (all 19 sections); cross-checked the sandbox scaffold (prisma/schema.prisma, src/lib tree) and the master-prompt reference material in upload/ to mirror the user's Electron architecture diagram.
    96→- Wrote docs/05-SYSTEM-ARCHITECTURE.md: 4 mermaid diagrams (high-level system, Electron process architecture mirroring main→preload contextBridge→renderer→domain→Dexie/IndexedDB+sync engine→cloud API→PostgreSQL+RLS and main→native services→print/export/OS, web application, deployment incl. sandbox), source-of-truth master principle, offline-first pipeline (CANON §1), layered design with sync engine as separate bounded context, monorepo→sandbox mapping (CANON §2), ADR-001..007 each with Context/Decision/Consequences, API surface, offline/online behavior, security, error-handling, acceptance criteria.
    97→- Wrote docs/06-DATABASE-DESIGN.md: storage-tier ownership matrix, common sync-metadata block (CANON §3) + sync_state state diagram, big erDiagram covering all 17 required tables, full field tables for every CANON §7 entity (name/type/Postgres type/nullable/meaning), server-only tables (users, sessions, changelog, processed_op) with mini erDiagram, Supabase mapping + RLS policy summary (CANON §8), Dexie v1 index set verbatim + Postgres index strategy, retention & soft-deletion policy, dual migration strategy (Dexie versions + supabase/migrations 0001-0003 + schema_version handshake).
    98→- Wrote docs/07-AUTHENTICATION.md: guest-first model, dev auth (scrypt 16B salt/64B key, if_session httpOnly SameSite=Lax 30-day cookie, Session table, 10 req/min/IP token bucket), mermaid sequences for register→session→claim→sync, login, and auth-expiry mid-sync, workspace claim contract, Supabase Auth production design (email/password + Google OAuth, JWT→RLS), logout & account deletion, CSRF stance (SameSite=Lax + JSON-only APIs), error table, acceptance criteria.
    99→- Wrote docs/08-COMPANY-MANAGEMENT.md: full company_profiles field reference, CompanyView form UX sections + validation (GSTIN regex, INDIAN_STATES reference, PNG/JPEG ≤1MB dataURL branding), prefixes/numbering config, tax defaults (default_gst_rate_bps/price_includes_tax/enable_round_off), multi-workspace design (workspaces + workspace_members + future multi-company), company→documents flow diagram, save/sync sequence, offline (outbox op upsert company) vs online (server revalidate + ChangeLog) behavior, security, error rules, acceptance criteria.
   100→- Verified consistency against CANON throughout: exact names (sync_operations, sync_metadata, app_settings, if_session, number_reassigned, computeDocumentTotals, UnifiedDocumentModel), §19 limitations quoted, no invented endpoints or fields; docs cross-reference 06/07/14/16/17/18/19/29/30/32/35/36/39.
   101→
   102→Stage Summary:
   103→- Files written: docs/05-SYSTEM-ARCHITECTURE.md (flagship architecture, 4 diagrams, ADR-001..007), docs/06-DATABASE-DESIGN.md (17-store model + Postgres/RLS + migrations), docs/07-AUTHENTICATION.md (guest-first + dev auth + Supabase Auth, 3 sequences), docs/08-COMPANY-MANAGEMENT.md (profile reference + UX + flows).
   104→- All four docs follow the standard section set (Purpose/Scope/Business requirements/Technical design/Data models/API contracts/Offline/Online/Security/Error-handling/Acceptance criteria) and are fully CANON-consistent; worklog appended, nothing overwritten.
   105→
   106→---
   107→
   108→Task ID: 2-a
   109→Agent: D1-docs-writer
   110→Task: docs 01-04
   111→Work Log:
   112→- Read worklog.md and docs/_CANON.md in full; treated CANON (§1–§19) as the single source of truth for entities, field names (qty_milli / unit_price_paise / gst_rate_bps / *_paise / *_bps), sync protocol, lifecycles, phases, and §2 sandbox mapping.
   113→- Wrote docs/01-PROJECT-OVERVIEW.md: vision, problem statement, target users, goals/non-goals, environment matrix (Electron Win/Mac/Linux, web PWA, offline/online), golden-rule pipeline as mermaid flowchart, module map (CANON §2 fold), success metrics, phase summary (CANON §18), high-level architecture mermaid (Renderer UI → Domain → Dexie/IndexedDB → Outbox → Sync Engine → Cloud API → Postgres/RLS; Electron main+preload), plus overview-depth data/API/offline/online/security/error/acceptance sections.
   114→- Wrote docs/02-REQUIREMENTS.md: 13 functional modules with FR-IDs (FR-CMP/CUS/PRD/QTN/INV/GST/PAY/DSH/RPT/SET/SYN/AUT/PDF) each with acceptance statements; NFRs for performance, offline, security, WCAG 2.1 AA accessibility, data integrity (integer paise math per CANON §4), auditability, error handling; constraints quoting CANON §19 verbatim; module-graph mermaid; traceability matrix to CANON sections and deep-dive docs.
   115→- Wrote docs/03-FEATURES.md: catalog F-01…F-20 each with description, user story, offline behavior, online behavior, edge cases, status (Implemented in MVP / Designed extension point per CANON §19.3/§19.6); quotation and invoice state-machine mermaid diagrams per CANON §12; sync push/pull sequence diagram per CANON §9; conflict UI per §10; guest→claim per §11; numbering per §6; PDF per §13.
   116→- Wrote docs/04-TECH-STACK.md: justified every choice (Next.js 16 App Router + TS strict, Tailwind 4, shadcn/ui New York + lucide-react, RHF+Zod with document-editor controlled-state exception per §15, Zustand, Dexie/IndexedDB with explicit NO-SQLITE-in-MVP decision, Supabase Postgres+Auth+Storage+RLS, dev-cloud via Next API routes + Prisma/SQLite per §8, jsPDF+jspdf-autotable per §13, hand-rolled PWA SW, Electron+electron-builder hardening, Vitest/RTL/Playwright docs-defined per §19.5, GitHub Actions, Vercel, pnpm+Turborepo with §2 sandbox mapping table, version pinning policy); package dependency mermaid with layering rules.
   117→- Verified all four files start with a `# <Title>`, contain Purpose/Scope, the required section set where relevant, ≥1 mermaid diagram each, no placeholders, and exact CANON terminology.
   118→Stage Summary:
   119→- docs/01-PROJECT-OVERVIEW.md — product overview: vision, users, goals/non-goals, environments, golden-rule + architecture diagrams, phases, success metrics.
   120→- docs/02-REQUIREMENTS.md — full FR/NFR specification with stable REQ-IDs, acceptance statements, CANON §19 constraints, traceability matrix.
   121→- docs/03-FEATURES.md — 20-feature catalog with offline/online behavior, edge cases, status labels, and lifecycle/sync mermaid diagrams.
   122→- docs/04-TECH-STACK.md — every technology choice justified against CANON, monorepo/sandbox mapping, pinning policy, package dependency graph.
   123→
   124→---
   125→Task ID: 2-h
   126→Agent: D8-docs-writer
   127→Task: root docs (README/DEVELOPMENT/DATABASE/API/SECURITY/CHANGELOG)
   128→Work Log:
   129→- Read worklog.md and docs/_CANON.md in full (§1–§19); verified project scaffold (package.json scripts, prisma/, electron/, supabase/, src/lib layout) and system date.
   130→- Wrote README.md: product identity + one-liner, Next.js/TypeScript/MIT shields badges, ✓-style grouped features, compact mermaid architecture (UI → Domain → Dexie → Outbox → Sync → Cloud, Electron beside), quick start (bun install → db:push → dev, guest mode + Load sample data + register-to-sync), project structure tree matching CANON §2 sandbox mapping, documentation index table listing all 41 docs (01–41) with one-line summaries, roadmap summary (Supabase swap, payments, sharing, SaaS, testing/release), MIT license, GST non-compliance disclaimer quoting CANON §19.2.
   131→- Wrote DEVELOPMENT.md: prerequisites, install/env (points to .env.example + docs/38), scripts table (dev/lint/db:push…), code style (strict TS, no any, domain-layer rule, integer paise/milli/bps + half-up, YYYY-MM-DD date strings, Zod both ends, server recompute), Dexie migration recipe (append db.version(n+1), never mutate v1), 6-step add-a-sync-entity checklist (Dexie table → zod schema → repository → server model → ChangeLog/outbox → docs), dev-cloud note (API routes auto-run), Electron dev workflow (ELECTRON_START_URL, sandbox cannot execute → electron/README.md), conventional commits, PR checklist.
   132→- Wrote DATABASE.md: three-store overview (Dexie local source of truth / Prisma-SQLite dev cloud / Supabase Postgres + RLS), common metadata + sync_state conventions, all-17-table v1 schema table with indexes, index strategy rationale, ChangeLog/ProcessedOp explanation, op-push + cursor-pull sync relationship, common operations (db:push, db:reset, Settings → Data export/import); links docs/06 + docs/16.
   133→- Wrote API.md: base URL, session-cookie auth (if_session, scrypt, 10 req/min/IP), CANON §14 endpoint table, `{error, code}` error format + status table, push/pull sync envelope quick reference with server rules, rate limits/batch bounds, curl quickstart (health, register, login, claim, push, pull).
   134→- Wrote SECURITY.md: CANON §16 baseline reproduced as area × measure × status table (honest statuses: implemented / migrations provided / scaffold / defined), password storage (scrypt details), session handling (32-byte token, httpOnly SameSite=Lax 30-day, needs_reauth), server-side authorization + RLS roles, money integrity (server recompute), Electron hardening list, secrets warning box (service keys server-only), private reporting policy with security.txt deployment note; links docs/29.
   135→- Wrote CHANGELOG.md: Keep-a-Changelog format, `## [0.1.0] - 2025-09-21` with Added (offline-first core, domain engine, quotation/invoice lifecycles, PDF, dashboard/reports/CSV, sync engine + dev cloud + auth + claim, conflicts, settings, PWA, Electron scaffold, Supabase migrations + RLS, 41 docs), Changed (hash-SPA routing, shared editor/engine), Security (hashing/sessions/validation/RLS/Electron/secrets/audit) — factual, no marketing.
   136→- Consistency pass: all content derived from docs/_CANON.md (§1,§2,§4,§6,§7,§8,§9,§10,§11,§13,§14,§15,§16,§17,§19); removed a fabricated release URL from CHANGELOG and a LICENSE-file link from README (only the 6 permitted files were written).
   137→Stage Summary:
   138→- Files written: /home/z/my-project/README.md, /home/z/my-project/DEVELOPMENT.md, /home/z/my-project/DATABASE.md, /home/z/my-project/API.md, /home/z/my-project/SECURITY.md, /home/z/my-project/CHANGELOG.md. No other files modified.
   139→
   140→---
   141→
   142→Task ID: 2-i
   143→Agent: E1-supabase-sql
   144→Task: Supabase migrations + RLS + seed
   145→Work Log:
   146→- Read worklog.md + docs/_CANON.md in full (§3 metadata, §4 money rules, §6 numbering, §7 entity model, §8 cloud schema, §9 sync protocol, §11 roles).
   147→- 0001_init.sql: pgcrypto; 17 tables (profiles, workspaces, workspace_members, company_profiles, customers, products, quotations(+items), invoices(+items), payments, tax_rates, document_sequences, attachments, audit_logs, change_log identity-PK, processed_ops) — UUID PKs (gen_random_uuid), common metadata (created_at/updated_at/deleted_at/version/workspace_id CASCADE/origin_device_id), paise bigint + bps CHECKs, FK indexes + workspace/status/date indexes, set_updated_at() trigger attached to all 14 mutable tables via DO block. Circular quotation↔invoice FKs resolved with ON DELETE SET NULL so workspace cascade deletes work.
   148→- 0002_rls.sql: RLS enabled on all 17 tables; SECURITY DEFINER STABLE helpers is_workspace_member(uuid) + has_role(uuid,text[]) + storage_path_workspace_id(text) (fail-closed uuid parse); 58 policies (SELECT=member, INSERT/UPDATE=OWNER/ADMIN/MEMBER, DELETE=OWNER/ADMIN; company_profiles INSERT=OWNER/ADMIN + MEMBER can UPDATE but not DELETE; workspace_members managed by OWNER/ADMIN; workspaces SELECT/UPDATE member only; change_log/processed_ops/document_sequences SELECT-only; audit_logs append-only with actor_user_id=auth.uid()); storage buckets company-assets+attachments (idempotent insert) with path-convention policies {workspace_id}/{entity}/{filename}.
   149→- 0003_functions.sql: round_half_up(numeric) + fiscal_year_of(date) helpers; allocate_document_number (insert-if-missing → SELECT FOR UPDATE → increment → '{prefix}/{FY}/{seq4}', prefix from company_profiles); sync_finalize_invoice (full SECURITY DEFINER finalize op: processed_ops idempotency → role ≥ MEMBER → CAS base_version → DRAFT-only → server-recomputes all totals/items per CANON §4 incl. charges tax + rupee round-off → allocates number → replaces items atomically → change_log + audit_logs + processed_ops; returns applied|duplicate|conflict|rejected JSON; execute granted to service_role only); handle_new_user() trigger on auth.users → profile + personal workspace + OWNER membership. Every object COMMENTed.
   150→- seed.sql: DO-block wrapped, idempotent, "DEV ONLY — do not run in production" guard (skips if slug exists); demo workspace acme-traders-demo, Acme Traders Pvt Ltd (Maharashtra 27), device-OWNER membership row, 3 customers (2× intra 27, 1× inter 29), 4 products at 5/12/18% (500/1200/1800 bps), 2 tax rates, 1 INVOICE 2025-26 sequence.
   151→- README.md: apply via supabase link/db push or psql, seed warning, RLS verification snippets (anon → 0 rows, authenticated → workspace-scoped), role matrix table, storage path convention, service-role key warnings, numbering/finalize integration notes.
   152→- Validation: no postgres in sandbox → structural checks on all 4 SQL files (dollar-quote pairing, paren balance per statement, string-literal quote parity) all pass; fixed one drafting artifact in 0003 during review.
   153→Stage Summary:
   154→- supabase/migrations/0001_init.sql — tables/indexes/triggers (CANON §7 snake_case)
   155→- supabase/migrations/0002_rls.sql — RLS helpers + 58 policies + storage buckets/policies
   156→- supabase/migrations/0003_functions.sql — numbering, finalize op, handle_new_user trigger
   157→- supabase/seed.sql — DEV ONLY demo dataset (guarded)
   158→- supabase/README.md — apply/verify/operate guide
   159→- Next actions (for orchestrator): run `supabase db push` against a real project to smoke-test; implement sync-push Edge Function calling sync_finalize_invoice; keep Prisma dev-cloud field names aligned with these tables.
   160→
   161→---
   162→Task ID: 2-j
   163→Agent: E2-electron-scaffold
   164→Task: Electron desktop scaffold (CANON §2 apps/desktop → electron/)
   165→Work Log:
   166→- Read worklog.md + docs/_CANON.md in full (§2 mapping, §13 PDF, §16 security baseline, §17 desktop offline, §19.6 scaffold scope).
   167→- Created the security-hardened Electron scaffold entirely inside electron/ (root package.json/tsconfig/eslint untouched, Next build unaffected).
   168→- package.json (`@invoiceflow/desktop`, main dist/main.js, compile/start/package scripts, electron ^33 / electron-builder ^25 / typescript ^5 / @types/node ^22 — deps NOT installed in sandbox) + tsconfig.json (ES2022/commonjs/strict, noUnusedLocals/Parameters).
   169→- main.ts: single-instance lock; BrowserWindow 1440×900 (min 1024×700, show:false) with contextIsolation+sandbox+nodeIntegration:false+webSecurity; ready-to-show → show+maximize; renderer modes (ELECTRON_START_URL dev/remote → embedded static export via file:// → localhost:3000 fallback); strict will-navigate allow-list (hash routing unaffected, file:// only inside bundle); setWindowOpenHandler deny + shell.openExternal https allow-list; CSP injection via onHeadersReceived (exact CANON §16 policy; dev adds 'unsafe-eval' for HMR); deny-all permission handler; will-attach-webview blocked; lifecycle (window-all-closed/activate/will-quit), menu wiring, graceful dialog.showErrorBox for fatal errors (uncaughtException/unhandledRejection/render-process-gone).
   170→- preload.ts: self-contained (sandbox-safe, only `electron` require in emitted JS — verified) contextBridge `window.invoiceflow`: isDesktop/platform/appVersion (one-shot sync bootstrap channel, since `app` is not importable in sandboxed preloads), getDeviceInfo/savePdf/printPdf/exportFile/openExternal/onNetworkChange (returns unsubscribe); all args validated + structuredClone'd before IPC.
   171→- ipc/channels.ts: IPC_CHANNELS const object (required set + 3 documented deviations: app:get-version, menu:export-pdf, app:open-external — each justified in comments), request/response interfaces, type guards for every payload, EXTERNAL_HOST_ALLOWLIST + isAllowedExternalUrl.
   172→- ipc/handlers.ts: registerIpcHandlers(getMainWindow) — every channel type-guards `unknown` payloads; app:get-device-info (os/hostname/release/versions), pdf:save, pdf:print, file:export, app:open-external (authoritative allow-list), updater:check; only user-chosen paths ever returned.
   173→- services: file-service (savePdf → documents-dir save dialog + writeFile, writeTempPdf randomized temp, printPdf via temp file + shell.openPath with documented printToPDF trade-off, sanitizeFileName), menu-service (File: New Invoice/Print/Export PDF/Quit; Edit/View/Window/Help roles, devtools+reload dev-only), network-service (net.online + DNS probe of app origin every 15 s → network:change push, rebroadcast on did-finish-load), update-service (INVOICEFLOW_UPDATE_URL JSON {version,url} stub with timeout/size-cap/https-redirects, semver-ish compare; electron-updater marked Phase 8).
   174→- invoiceflow.d.ts: global `Window.invoiceflow: InvoiceFlowBridge | undefined` typing for renderer runtime detection.
   175→- electron-builder.yml: appId com.invoiceflow.app, NSIS (oneClick:false, dir choice), DMG (business, hardenedRuntime + signing/notarization notes), AppImage+deb (Office), publish github, optional renderer/** embed comment.
   176→- README.md: mermaid architecture (Main → preload bridge → Next renderer → domain → Dexie → sync → cloud; main → native services), runtime detection, shared-code story, security-model table (CANON §16), dev workflow (ELECTRON_START_URL=http://localhost:3000 npm start), per-OS packaging + code-signing notes, IPC table, limitations/roadmap. Plus electron/.gitignore.
   177→- VERIFICATION (no electron dep in sandbox): type-checked all of electron/src under strict TS 5.9 against a faithful Electron-33 typings stub in a temp harness — tsc exit 0, zero errors; verified tsc emit layout produces dist/main.js + dist/preload.js exactly as package.json expects; verified emitted preload.js requires only `electron` (type-only imports erased → sandbox-safe single file); JSON/YAML validated; grep confirms zero `any` usage.
   178→- NOTE FOR ORCHESTRATOR: root tsconfig `include: ["**/*.ts"]` currently pulls electron/src into Next's type-check → add "electron" to root tsconfig `exclude` (and eslint ignores) as planned; no other root changes needed.
   179→Stage Summary:
   180→- Files written: electron/package.json, electron/tsconfig.json, electron/electron-builder.yml, electron/README.md, electron/.gitignore, electron/src/main.ts, electron/src/preload.ts, electron/src/invoiceflow.d.ts, electron/src/ipc/channels.ts, electron/src/ipc/handlers.ts, electron/src/services/{file-service,menu-service,network-service,update-service}.ts.
   181→- Desktop shell is scaffold-complete and strict-type-verified; renderer remains the untouched Next.js app; worklog appended, nothing outside electron/ modified.
   182→
   183→---
   184→Task ID: 2-f
   185→Agent: D6-docs-writer
   186→Task: docs 28-34 (retry — previous attempt timed out before writing any files; all 7 written fresh this run)
   187→Work Log:
   188→- Read worklog.md (all entries) and docs/_CANON.md completely (§1–§19, 266 lines) before writing; cross-checked style/depth against existing docs (17-SYNC-ENGINE.md, 07-AUTHENTICATION.md) and prior agents' entries (esp. E1's RLS policy summary, E2's Electron hardening) so 28-34 stay consistent with already-written docs 05-27, 35-41 and root docs.
   189→- Wrote 28-TEAM-MEMBERS.md: OWNER>ADMIN>MEMBER>VIEWER definitions; full 9-capability × 4-role permission matrix (view/create/edit/delete/finalize/pay/manage members/manage billing/manage workspace) with the server enforcement column citing CANON §8 + §9.7 (role ≥ MEMBER writes) and E1's policy set; MVP reality per §19.7 (single local OWNER row keyed by device_id, no membership UI); dev-cloud enforcement sequence + Supabase RLS helpers (is_workspace_member / has_role, fail-closed); future invite design (workspace_invites table + 6 endpoint extension table reusing docs/30 conventions); data model + permission-model flowchart mermaid.
   190→- Wrote 29-SECURITY.md: auth hardening table (scrypt 16B salt/64B key/timingSafeEqual dev, Supabase prod, if_session httpOnly SameSite=Lax Secure-on-HTTPS, 10 req/min/IP + 429 Retry-After); server-side authorization (membership→role→owner ladder, RLS backstop, owner_user_id server-written); Zod both ends + server money recompute (CANON §4/§9.4); injection table (Prisma parameterization, React escaping + dangerouslySetInnerHTML ban + CANON §16 CSP verbatim); CSRF stance (SameSite=Lax + JSON-only + no CORS, no GET mutations); Electron hardening table (contextIsolation/sandbox/nodeIntegration:false, typed contextBridge, will-navigate + https allow-list, deny-all permissions); secrets server-only; upload validation PNG/JPEG ≤1 MB (+2 MB attachments); audit_logs append-only; CI dependency audit; account deletion (cascade server-side, local remains) + JSON/CSV export; 15-row threat × vector × mitigation table; 10 acceptance criteria.
   191→- Wrote 30-API-DESIGN.md: conventions (JSON, {error, code?} envelope, CANON §14 status codes, cookie/JWT auth, no CORS, UUID/ISO/paise types); full contracts for ALL 9 CANON §14 endpoints with request/response JSON, status-code tables and curl examples (health, register w/ auto-claim, login w/ Set-Cookie, logout, session, DELETE account 204, workspace/claim, sync/push, sync/pull incl. 409 schema_version); entity×action validity matrix for all 7 sync entities (invoice finalize=number allocation, cancel only when paid_total=0; quotation finalize=mark SENT; company/workspace upsert-only…); per-op result statuses applied/duplicate/conflict/rejected/number_reassigned each with a JSON example and client handling; offline/online behavior per CANON §9.
   192→- Wrote 31-UI-UX.md: layout anatomy (fixed slate-950 sidebar w/ emerald accents, topbar w/ ⌘K+OfflineBadge+SyncPill+theme+user menu, sticky footer via min-h-screen flex-col + mt-auto showing version•sync•workspace, mobile Sheet drawer); color tokens (emerald-600/500 primary, slate neutrals, status map paid=emerald unpaid=amber overdue=red draft=slate + full status-badge table, NO blue/indigo, chart series rule); Geist typography + p-4/p-6 & gap-4/6 spacing; 25-component inventory (StatCard…ConflictDialog); 12 screen specs (dashboard 8 KPIs, shared DocumentEditor invoice/quotation w/ Save & finalize / Mark SENT / Convert, settings tabs, onboarding); interaction patterns (⌘K palette, sonner, AlertDialog gates, skeletons, sync dots); responsiveness breakpoints + max-h-[70vh] tables; WCAG 2.1 AA section (focus rings, ARIA, contrast, reduced-motion); next-themes theming; navigation mermaid.
   193→- Wrote 32-ROUTES.md: production App Router table — all 21 routes (login…settings/security) with Auth + Workspace guard columns and notes (team route marked future per §19.7); sandbox hash-route table — all 15 CANON §15 routes mapped to production counterparts with honest diffs (register→#/login mode, product detail→dialog, settings splits→tabs, workspaces deferred); preview constraint explained per CANON §2; src/lib/router.ts design (parse/route table/navigate/hashchange/Zustand store/new-before-:id ordering/onboarding+unknown→dashboard rules); route-protection mermaid (client guards = UX, server = boundary); deep-link/reload behavior.
   194→- Wrote 33-VALIDATION.md: schemas.ts architecture (metadata/documentItem/docCharge layering, int contracts, superRefine cross-field rules, three consume sites incl. sync server, additive-only versioning); per-entity rule tables (customer GSTIN regex from CANON §5 + INDIAN_STATES ref, phone 10-digit, PIN 6; product/company incl. IFSC/PAN regex + branding ≤1 MB; payment >0 and ≤ balance; items qty_milli>0, discount 0-10000 bps, gst_rate_bps ∈ {0,250,500,750,1200,1800,2800}); document rules (≥1 item, customer, place of supply hard; due_date ≥ invoice_date + valid_until ≥ quotation_date warnings); lifecycle gates (finalized immutable, cancel w/o payments); server revalidation+recompute pipeline order and overwrite semantics; error UX (inline vs toast/banner mapping); 8 acceptance criteria.
   195→- Wrote 34-ERROR-HANDLING.md: 4-class taxonomy (domain w/ "Cannot edit finalized invoice", storage banners incl. QuotaExceeded, sync incl. needs_reauth/conflict/schema_version guard, server) + classification flowchart mermaid; canonical server-status→client-behavior table (400/401/403/404/409/429/500 with auto-retry column); CANON §9 retry schedule verbatim (2^n×2s cap 10 min, 8 attempts→failed, batch independence, pruning); one-pattern-per-class presentation table; offline silent-queue suppression scoped to connectivity only; logging (audit_logs, correlation ids, redaction); 8 acceptance criteria.
   196→- Consistency pass: exact CANON terminology throughout (if_session, number_reassigned, sync_state values, sync_operations enums, computeDocumentTotals, INDIAN_STATES, ProcessedOp, pull cursor, backoff formula); no invented endpoints/fields/routes; extension points explicitly banner-marked; no other files touched.
   197→Stage Summary:
   198→- docs/28-TEAM-MEMBERS.md — roles + permission matrix + RLS/push enforcement + invite extension-point design (177 lines)
   199→- docs/29-SECURITY.md — CANON §16 expanded: auth/authz/validation/injection/CSRF/Electron/secrets/uploads/audit/deletion + threat model (156 lines)
   200→- docs/30-API-DESIGN.md — all 9 endpoints contracted with examples + sync op validity matrix + 5 result-status examples (308 lines)
   201→- docs/31-UI-UX.md — design tokens, components, 12 screen specs, interactions, a11y, theming, nav diagram (172 lines)
   202→- docs/32-ROUTES.md — production guard table + sandbox hash mapping + router design + protection flow (151 lines)
   203→- docs/33-VALIDATION.md — shared Zod layer, per-entity rules, document rules, server recompute, error UX (180 lines)
   204→- docs/34-ERROR-HANDLING.md — taxonomy, status mapping, backoff, UX patterns, logging, classification diagram (153 lines)
   205→
   206→---
   207→Task ID: 2-c
   208→Agent: D3-docs-writer
   209→Task: docs 09-13 (retry)
   210→Work Log:
   211→- Read worklog.md and docs/_CANON.md completely (all 266 lines) before writing; treated CANON as the single source of truth (§3 metadata, §4 money math, §5 GST, §6 numbering, §7 entities/Dexie, §9 sync, §10 conflicts, §12 lifecycles, §13 PDF, §15 UI, §16 security, §19 limitations); cross-checked sibling docs (01-08, 14-27, 35-41) plus supabase/migrations/0001 to avoid contradictions — notably adopted docs/21's `invoice_items.product_id` picker-stamped linkage and docs/35/36's canonical totals fixture (raw 235999 → round-off +1 → 236000).
   212→- Wrote docs/09-CUSTOMER-MANAGEMENT.md: full `customers` field reference (auto `CUS-0001` codes — informational, never reused, UUID id is identity, immutable once set; BUSINESS|INDIVIDUAL; GSTIN per §5 regex + INDIAN_STATES prefix existence; 10-digit phone; state picker over `INDIAN_STATES`), list UX (search over code/gstin/phone/name, type/state filters, 10/page, show-deleted), RHF+zod form, non-blocking duplicate detection (same phone/gstin/name with Open links), per-customer transaction history (invoiced/collected/outstanding from snapshots), soft-delete + restore rules (OWNER/ADMIN; documents keep `customer_name_snapshot`), save/sync flowchart + erDiagram, error table, acceptance criteria.
   213→- Wrote docs/10-PRODUCT-SERVICE-MANAGEMENT.md: full `products` fields (unit default NOS, `selling_price_paise` req ≥ 0, `gst_rate_bps` from company default within the workspace's configured rate set, `price_includes_tax` init-from-company, `active`), goods+services single catalog (HSN/SAC format-only, no directory in MVP), duplicate detection = same name+SKU → sonner warning toast with Save-anyway override, usage history via picker-stamped `product_id` with the "Ad-hoc line items" bucket and an explicitly justified additive-field deviation (Supabase 0001 lacks the column → flagged as orchestrator follow-up migration per docs/16 strategy), active/inactive picker semantics (existing snapshots untouched), catalog→document flowchart, acceptance criteria.
   214→- Wrote docs/11-QUOTATION-MODULE.md: lifecycle stateDiagram (DRAFT→SENT→ACCEPTED/REJECTED; SENT→EXPIRED lazy via `effectiveStatusOf`; ACCEPTED→CONVERTED; terminal EXPIRED/CONVERTED; duplicate from any state), shared editor UX (customer picker + quick-create, place of supply defaulting/override, qty_milli/unit_price_paise/discount_bps/gst_rate_bps lines, charges, tax-inclusive toggle, live totals), numbering (provisional DRAFT-xxxxxxxx; mark-SENT = finalize allocating `QT/{FY}/{seq4}` online (server wins) or offline (local allocation, fast-forward or `number_reassigned` adoption)), ONE worked numeric example with exact integer math reproducing the docs/35/36 fixture (2 lines, 5% discount, ₹50 taxable charge, intra 18%: raw 235999 → round-off +1 → 236000, in-words line), duplicate/PDF (QUOTATION title, no bank block), convert-to-invoice sequenceDiagram (one Dexie tx: invoice DRAFT copying items/charges/notes/terms + source_quotation_id → quotation CONVERTED + converted_invoice_id → two outbox ops → audits on both), immutability after SENT (status-only diffs; duplicate→edit→resend corrections), offline/online, error table, acceptance criteria.
   215→- Wrote docs/12-INVOICE-MODULE.md: lifecycle stateDiagram (DRAFT→FINALIZED→PARTIALLY_PAID→PAID; FINALIZED|PARTIALLY_PAID→CANCELLED guarded by paid_total_paise = 0; VOIDED future), editor with due date (overdue anchor convention shared with docs/20/21) and the shared line-item engine, finalize sequenceDiagram with all three branches per CANON §6 (online server allocation in serializable tx / offline one-transaction local allocation / `number_reassigned` adoption with notice), payments per docs/27 Part 1 (dialog with amount ≤ balance, method enum CASH|BANK_TRANSFER|UPI|CHEQUE|CARD|OTHER, recalc pseudocode, partial payments, overpayment-as-Advance, two-op writes + PAYMENT audit, history + #/payments), worked payment example on the ₹2,360.00 invoice, duplicate, TAX INVOICE PDF (bank block, Rs. limitation), conversion from ACCEPTED quotation, immutability + correction workflow (duplicate→edit→reissue; cancel keeps number), offline/online, error table, acceptance criteria.
   216→- Wrote docs/13-GST-MODULE.md: §5 GSTIN regex with structure breakdown (format-only MVP, no checksum/directory), state codes referenced exclusively via `src/lib/domain/gst.ts` `INDIAN_STATES` (38 codes, `ut` flag; examples only, nothing re-listed), intra/inter rule (supplier `state_code` vs `place_of_supply_code`, PoS defaults to billing state, `tax_mode` snapshot), verbatim CANON §4 half-up formulas with three worked examples — intra odd-paise (17099 → cgst 8550 / sgst 8549, SGST carries remainder), inter (same canonical economics → igstTotal 35100, grand total unchanged at 236000), tax-inclusive (₹999 tag → taxable 84661, tax 15239 → 7620/7619), HSN/SAC snapshot rules, versioned `tax_rates` (retention-based versioning, snapshots authoritative), GST summary report outline deferring to docs/21 (month rows, CGST/SGST-UTGST/IGST columns from snapshots, charge-tax reconciliation note), verbatim §19.2 compliance disclaimer, tax decision flowchart (exclusive/inclusive → intra/inter → split → totals → server recompute), acceptance criteria.
   217→- Consistency pass on all five: identical section skeleton (Purpose/Scope/Business requirements/Technical design/Data models/API contracts/Offline/Online/Security/Error-handling/Acceptance criteria/References), 12 numbered sections + ≥1 mermaid diagram each (7 blocks total), all money in paise/bps/milli with half-up, ops/results wording matching CANON §9 (`applied|duplicate|conflict|rejected|number_reassigned`), lifecycle transitions matching CANON §12 and docs/03, no placeholders.
   218→- Post concurrent-appends alignment pass (2-f landed while this entry was being appended): docs/09 phone (10-digit), GSTIN INDIAN_STATES prefix check, required `state_code`, immutable `code` and docs/33 §5.1 references aligned to docs/33-VALIDATION.md; docs/10 + docs/13 `gst_rate_bps` validation aligned to docs/33's workspace-configured rate set `{0,250,500,750,1200,1800,2800}` (custom rates enter via the versioned `tax_rates` table — preserves CANON §5 configurability); my worklog entry re-anchored to the true end of the file after the concurrent append landed above it (no content overwritten).
   219→Stage Summary:
   220→- Files written: docs/09-CUSTOMER-MANAGEMENT.md, docs/10-PRODUCT-SERVICE-MANAGEMENT.md, docs/11-QUOTATION-MODULE.md, docs/12-INVOICE-MODULE.md, docs/13-GST-MODULE.md (5/5 complete; worklog appended at end, nothing overwritten).
   221→- Note for orchestrator: item-table `product_id` (docs/10 §4.4, consumed by docs/21) needs the additive cloud column migration + optional Dexie v2 index per docs/16.
   222→
   223→---
   224→Task ID: 3-b
   225→Agent: F3-settings
   226→Task: Settings view
   227→Work Log:
   228→- Read worklog.md (full tail), docs/_CANON.md, docs/17-SYNC-ENGINE.md and docs/39-BACKUP-RESTORE.md before coding; cross-read existing code to match conventions: src/lib/hooks/app-hooks.ts (useActiveWorkspace/useUser/useAppSetting), src/lib/stores/app-store.ts (sync status, needsReauth, storageAvailable), src/lib/db/db.ts (17-table Dexie schema), src/lib/sync/engine.ts (runSync/countPending), src/lib/sync/conflicts.ts (resolveConflict/retryFailedOp/discardFailedOp), src/lib/sync/client.ts (apiLogout/apiDeleteAccount), src/lib/device.ts, src/lib/router.ts (navigate), app-shell.tsx + sync-pill.tsx (pill/status styling) and the full shadcn/ui set.
   229→- Created exactly ONE new file src/components/views/settings-view.tsx ('use client'); no other file touched, no routes added. Exports `SettingsView({ initialTab?: 'preferences'|'sync'|'data'|'security' })` rendering a 4-tab Tabs layout (Preferences | Sync | Data | Security) designed to render inside AppShell content area.
   230→- Preferences: theme RadioGroup (Light/Dark/System) via next-themes useTheme with hydration-safe `useSyncExternalStore` mounted flag (avoids the react-hooks/set-state-in-effect lint error that the classic useState-in-effect mounted pattern triggers); App & device card (v0.1.0, short device id + copy-to-clipboard with aria-label, storage mode "IndexedDB (persistent)" or amber Alert when useAppStore storageAvailable is false); link-style Button → navigate('company').
   231→- Sync: status card (cloud-link badge from user+workspace.cloud_linked_at, last sync via formatDistanceToNowStrict, pending count from store.sync.pending, "Sync now" → runSync() with spinner, last-error line, needsReauth amber Alert with sign-in link → navigate('login'), guest hint); Outbox table via useLiveQuery over db.sync_operations (workspace-filtered, newest-first) with columns Op/Entity/Action/Status/Attempts/Created in a max-h-[50vh] overflow-y-auto scrollbar-thin container, "All operations synced" empty state, skeletons while loading, 100-row render cap with "+N older" footer; status badges pending=amber / in_flight=TEAL / done=emerald / failed=red / conflict=orange (no blue anywhere); Conflicts card (per-op AlertDialog confirm → resolveConflict('mine'|'theirs'|'delete') with Local-vX/Server-vY diff hint read from payload/server_record); Failed ops card (last_error shown, Retry → retryFailedOp, Discard → AlertDialog confirm → discardFailedOp), all with toasts.
   232→- Data: Export builds {meta:{app:'invoiceflow',version:1,exported_at,device_id}, tables:{...all 17 Dexie tables via db.tables → toArray()}} → Blob download `invoiceflow-backup-YYYYMMDD.json` (task-directive filename; docs/39's HHmmss suffix intentionally simplified to match the orchestrator spec) and records app_settings['last_backup_at'] (docs/39 §4.3) shown as "Last export"; Import via hidden file input (accept="application/json") → validate meta.app==='invoiceflow' + meta.version≤1 → AlertDialog merge-policy confirm ("keeps newer version per record, never deletes, unknown tables skipped") → per-row version-aware merge (file wins only when version/updated_at newer; document_sequences next_seq=max(local,file) per CANON §6; current device's app_settings['device_id'] never overwritten) → summary toast {inserted/updated/skippedNewer} + reload-suggestion toast with Reload action; Danger zone red-bordered card with TWO-STEP AlertDialog confirm → db.delete() → location.reload().
   233→- Security: Account card — signed-in shows name/email/id, Sign out (apiLogout → setUser(null)) and Delete account behind TWO AlertDialog steps (apiDeleteAccount → toast → setUser(null)); guest state shows the exact "guest workspace" copy + Sign in / Create account buttons → navigate('login'); Privacy card with 3 icon bullets (IndexedDB local-first, cloud sync only when signed in, export anytime).
   234→- Style compliance: Card p-4/p-6 + gap-4/6, text-sm font-semibold section headings, text-xs muted descriptions, tabular-nums on numerics, responsive grid lg:grid-cols-2 where apt, scrollbar-thin on the outbox scroll area, aria-labels on all icon-only buttons, emerald/amber/orange/teal/red palette only (zero blue/indigo).
   235→- Quality gates: `bunx tsc --noEmit | grep settings-view` → clean (fixed a Table name collision with the shadcn Table via `import type { Table as DexieTable } from 'dexie'`, and a fileWins undefined-narrowing error); `bunx eslint src/components/views/settings-view.tsx` → clean; dev.log shows successful compiles. Pre-existing errors in repositories.ts / pdf/render.ts / api routes / document-editor.tsx belong to other agents and were NOT touched.
   236→Stage Summary:
   237→- src/components/views/settings-view.tsx (1233 lines) — complete 4-tab Settings view: Preferences (theme, app/device info, storage warning, company link), Sync (status + Sync now + reauth alert, live outbox table, conflict resolver, failed-op retry/discard), Data (JSON export/import with version-aware merge + double-confirmed clear-local-data), Security (sign out / double-confirmed account deletion / guest onboarding / privacy) — typed strict, no `any`, lint+tsc clean, ready for AppShell wiring (`<SettingsView initialTab={...} />` from the settings route).
   238→
   239→---
   240→Task ID: 3-a
   241→Agent: F2-reports
   242→Task: Reports view
   243→Work Log:
   244→- Read worklog.md, docs/_CANON.md (§3 dates, §4 money, §7 tables, §12 lifecycles), docs/21-REPORTS.md, plus existing conventions: app-hooks.ts (useActiveWorkspace/useCompany), db/db.ts + row-types.ts (Dexie rows, charges_json), domain/money.ts, lib/date.ts, lib/csv.ts, app/empty-state.tsx, app/app-shell.tsx, ui card/table/tabs/badge/skeleton, app-store (booted flag), globals.css (--chart-1 emerald / --chart-2 amber oklch tokens, .scrollbar-thin utility).
   245→- Created exactly one file: src/components/views/reports-view.tsx ('use client', default + named ReportsView export; no routes, no other files touched).
   246→- Period controls card: two <Input type="date"> (from/to) with lazy useState defaults = fyStart/fyEnd(todayStr()) (current FY per BR-3, lint-clean alternative to setState-in-effect), swapped-range amber notice, live period readout, company-scoped description.
   247→- Five shadcn Tabs — Sales | GST Summary | Outstanding | Customers | Products — each a Card (p-4 paddings, gap-4) with its own Export CSV button (disabled until data loads; sonner toast on export; filename `invoiceflow-{report}-{from}_to_{to}.csv`; UTF-8 BOM/CRLF via lib/csv toCsv; money as fixed-2 decimal rupees, qty ≤3 decimals, docs/21 column dictionaries, TOTAL rows).
   248→- Sales: recharts AreaChart (ResponsiveContainer, gradients over var(--chart-1)/var(--chart-2), compact-paise Y ticks, ₹ tooltip) of monthly Invoiced (Σ grand_total_paise of FINALIZED/PARTIALLY_PAID/PAID by invoice_date month) vs Collected (Σ payments amount_paise by paid_at month) + Month/Invoiced/Collected table with sticky totals footer.
   249→- GST Summary: per-month matrix (Docs, Taxable, CGST, SGST/UTGST, IGST, Total GST) from authoritative snapshot columns of FINALIZED+ invoices, grand-total row, permanent "not a statutory GST return" disclaimer.
   250→- Outstanding: FINALIZED/PARTIALLY_PAID with balance>0; due_date anchor (fallback invoice_date), max(0, days past due) via Date.UTC diff; 0-30/31-60/61-90/90+ buckets; aging summary strip (balance+count per bucket, total tile), badges emerald/amber/orange/red, sorted due_date asc, totals footer.
   251→- Customers: grouped by customer_name_snapshot — Invoices count, Invoiced (sorted desc, proportional bg-chart-1 bar), Collected (payments joined via parent invoice, orphans skipped with dev-only warn), Outstanding; code from customers table in CSV.
   252→- Products: invoice_items of base invoices grouped by description — Σ qty_milli (formatQty), taxable/tax/revenue (Σ total_paise), HSN/SAC snapshot, sorted revenue desc.
   253→- States: booted/company-gated Skeletons; EmptyState (no workspace → Dashboard CTA, no company → My Company CTA via hash navigate); per-tab "Pick a date range" / empty states with "Widen the date range" action (±366d around FY bounds); useLiveQuery on [wsId, from, to], aggregations pure + memoized, tabular-nums, max-h-96 overflow-auto scrollbar-thin, dark-mode token classes, no blue/indigo.
   254→- Verification: `bunx tsc --noEmit | grep reports-view` → clean (exit 1, no matches); `bunx eslint src/components/views/reports-view.tsx` → 0 problems; dev.log compiles green.
   255→Stage Summary:
   256→- Reports view complete at src/components/views/reports-view.tsx (~1080 lines): FY-default period filter + 5 tabs (Sales chart+table, GST Summary matrix+disclaimer, Outstanding aging with bucket strip, Customers by revenue, Products by qty/revenue), per-tab WYSIWYG CSV exports per docs/21 spec, live Dexie queries, full loading/empty/edge states — typed strict, no `any`, lint+tsc clean, ready for AppShell wiring (`<ReportsView />` in the #/reports route; title "Reports" comes from the shell).
   257→
   258→---
   259→Task ID: 3 (main build)
   260→Agent: Z.ai Code (orchestrator)
   261→Task: Build the complete InvoiceFlow application (domain, local DB, sync engine, dev cloud, all UI modules), verify end-to-end with agent-browser, and fix all defects found.
   262→
   263→Work Log:
   264→- Domain layer (src/lib/domain/): types.ts (17 entities, sync metadata), money.ts (integer paise, Indian words), gst.ts (38 state codes, GSTIN regex, CGST/SGST/IGST split with odd-paise→SGST rule), numbering.ts (FY Apr–Mar, provisional DRAFT-XXXXXXXX), schemas.ts (shared Zod, client+server), documents.ts (computeDocumentTotals — single source of truth for client UI, repositories, sync server, PDF).
   265→- Local DB (src/lib/db/): Dexie schema v1 (17 tables + compound indexes per CANON §7), repositories (transactions + outbox enqueue + audit logs for every mutation), demo seeder (company/customers/products/quotations/invoices/payments spanning 6 months).
   266→- Sync engine (src/lib/sync/): op-based outbox, push (batches of 25) → pull (ChangeLog cursor), exponential backoff (2^n×2s, cap 10 min, 8 attempts → failed), 401→needs_reauth pause, conflict storage + resolution UI actions (keep mine/keep server's/delete), ProcessedOp idempotency, done-op pruning after 7 days.
   267→- Dev cloud (src/lib/server + src/app/api/): Prisma/SQLite mirroring Supabase schema; scrypt password hashing, httpOnly SameSite=Lax session cookies, in-memory rate limiting (auth routes); /api/health, /api/auth/{register,login,logout,session,account}, /api/workspace/claim, /api/sync/{push,pull}; server recomputes ALL financial totals via shared domain engine, CAS version conflicts, server-authoritative document numbering (allocate + fast-forward), finalized-doc immutability, payment-driven status recalculation with ChangeLog publication.
   268→- UI: emerald/slate theme (light+dark), AppShell (dark sidebar, topbar with ⌘K search, sync pill, theme toggle, user menu, sticky footer), onboarding (create company / load sample data), auth (guest-first + register/login with auto workspace claim), dashboard (8 KPIs, revenue chart, recent transactions), customers (cards + detail + history + soft delete), products (catalog + duplicate detection + usage), invoices (list/detail/editor, finalize, payments, duplicate, PDF, cancel), quotations (lifecycle, SENT numbering, convert-to-invoice with banner), payments, reports (5 tabs, FY range, CSV), settings (preferences/sync-with-conflicts+outbox/data backup JSON/security). PDF: jsPDF + autotable deterministic A4 renderer (Rs. limitation documented).
   269→- PWA: manifest.webmanifest + sw.js (cache versioning, prod-only registration). Electron scaffold by agent E2 (electron/ excluded from root tsconfig/eslint). Supabase migrations + RLS + functions + seed by agent E1.
   270→- Defects found via agent-browser E2E and fixed:
   271→  1. next/link intercepts hash-href clicks (no hashchange event) → plain <a> for hash navigation.
   272→  2. Prisma nested item creates must not carry the FK (Unknown argument invoiceId).
   273→  3. Rejected/conflict ops were persisted in ProcessedOp blocking retries → only successful outcomes stored; stored rejections are re-attempted.
   274→  4. Payment-recalc and status-transition upserts lacked items; op compaction dropped the original item-bearing create → items embedded in every document op; payment recalc is now a local projection + server-side recalculation with ChangeLog publication.
   275→  5. Charge tax was double-counted in grand totals → charges_tax_paise is display-only now.
   276→  6. Server parseCharges didn't accept serialized charges_json strings.
   277→  7. Quotation/document status hardcoded to DRAFT in server create/update branches → canonical status preserved (DRAFT|CONVERTED on upserts; FINALIZED only via finalize op).
   278→  8. Quotations never received official numbers → allocation at SENT (client) + server allocate/fast-forward.
   279→  9. Dexie transaction scopes extended for new table access (document_sequences, quotation_items, invoice_items).
   280→- E2E verified: seed → register → claim → 33/33 ops synced; server numbers INV/2026-27/0001-0006 + QT/0001-0003; statuses PAID/PARTIALLY_PAID/FINALIZED/DRAFT/CONVERTED all correct; paid == grand exactly; PDF preview renders; converted-quotation banner links to its invoice; reports tabs + CSV; settings outbox shows done ops; dashboard pill "Synced".
   281→
   282→Stage Summary:
   283→- InvoiceFlow 0.1.0 is functionally complete in the sandbox: offline-first local CRUD, GST engine (integer paise), document lifecycles, numbering, PDF, dashboard/reports, guest→account claim, real push/pull sync with idempotency + conflicts + backoff, dev cloud (Prisma/SQLite) mirroring production Supabase migrations (RLS ready).
   284→- Known notes: quotation numbers in the current demo dataset are consistent; historical demo rows created before the totals fix were rebuilt via a full clean reseed; sw.js registers only in production builds; tests are documented (docs/35, 36) per sandbox rules; Electron scaffold cannot execute in sandbox (documented in electron/README.md).
   285→
   286→---
   287→Task ID: 4 (QA + fixes + features round)
   288→Agent: Z.ai Code (orchestrator)
   289→Task: Assess project status, agent-browser QA, fix defects, add features + styling polish, verify end-to-end.
   290→
   291→Work Log:
   292→- Read worklog + repo. App was "functionally complete 0.1.0" but full browser QA found 3 real defects.
   293→- QA method: agent-browser walked dashboard/invoices/quotations/payments/customers/products/reports/settings, invoice detail, PDF preview, editor; pixel-sampled screenshots with PIL to diagnose color bugs; verified console; exercised sync end-to-end (seed → register → claim → push/pull → edit → sync; conflict resolution via UI).
   294→- DEFECT 1 (rendering): stale Turbopack CSS chunk served the default shadcn theme (near-white sidebar, `--primary #171717`, default charts) — the entire emerald/dark-sidebar design system was dead in the running app. Pixel sampling caught it (white `text-white` brand on `rgb(250,250,250)` sidebar). Fixed by touching globals.css to force recompile; theme restored everywhere (reports charts now use emerald/amber tokens).
   295→- DEFECT 2 (sync, MAJOR): every `enqueueOp` call passed the POST-save `record.version` as `base_version`; server CAS (`existing.version !== op.base_version`) therefore rejected the FIRST edit of any synced record as a spurious conflict ("Local v2 · Server v1"). Fixed all 14 call sites in repositories.ts to pass the pre-edit version (`existing?.version ?? 0` / `inv.version` / `q.version`; payments create base 0). Verified: UI edit → Synced with no conflict; the 5 demo-data conflicts created pre-fix were resolved through the Settings→Sync conflict UI (keep-mine → re-push applies).
   296→- DEFECT 3 (data): demo seeder raced on customer code allocation (5× CUS-0001). First attempt (promise-chain lock) was insufficient — reads serialized but writes still raced. Final fix: allocation happens INSIDE the same Dexie/IndexedDB readwrite transaction as the put (IDB serializes overlapping rw transactions). Verified: fresh concurrent seed yields unique CUS-0001..0005; also made seed save customers sequentially and made the code editable in the customer form (custom ledger codes allowed; blank = auto).
   297→- DEFECT 4 (types): pre-existing tsc error in setQuotationStatus — Dexie 6-table transaction now uses array-form overload. `bunx tsc --noEmit` is clean for src/ (only unrelated skills/ errors remain); `bun run lint` clean.
   298→- OPS: dev server was OOM-killed mid-round (dmesg: next-server 2.3GB RSS on 4GB box, Turbopack + ~40 fast-refresh cycles). Restarted; NOTE: plain `nohup bun run dev &` instances died between tool calls; the working invocation was `(bun run dev > /tmp/devlive.log 2>&1 &)`. Watch RSS after heavy edit rounds.
   299→- FEATURES: (1) Customer account statement in detail view — FY period filter, chronological debits/credits with running balance, Invoiced/Collected/Outstanding tiles, CSV export (numbers cross-checked vs customer outstanding ₹4,116). (2) WhatsApp-ready payment reminder on invoice detail — builds polite message with due-date/overdue days, paid/balance, bank details; clipboard copy verified ("Reminder copied" toast). (3) Dashboard "Top customers by outstanding" widget (amber bars, click-through). (4) Dashboard activity feed upgraded (action icons/labels). (5) Global keyboard shortcuts N/⇧N/D/I/Q/C/P/R/S (suppressed while typing/modal open), hinted in sidebar footer + listed in Ctrl+K palette (verified r/d).
   300→- STYLING: per-view enter animation via `.view-enter` keyframe keyed on route segment (reduced-motion safe); hover lift+shadow on stat/customer cards; `active:scale-[0.98]` on buttons; overflow-auto on invoice/quotation/payment tables for mobile.
   301→- Files touched: src/lib/db/repositories.ts, src/lib/db/seed.ts, src/components/views/{customers-view,invoice-detail-view,dashboard-view,quotations-view,payments-view,invoices-view}.tsx, src/components/app/{app-shell,search-dialog,stat-card}.tsx, src/lib/hooks/use-shortcuts.ts (new), src/components/ui/button.tsx, src/app/globals.css, CHANGELOG.md (0.2.0 entry).
   302→- Verified E2E after fixes: fresh profile → onboarding → sample data (unique codes) → register owner2@acmetraders.in → claim → all ops synced → edit customer → Synced clean. Cloud DB now holds 2 workspaces (the old one is orphaned dev data with the historical CUS-0001 dupes; ignore or wipe via prisma migrate reset if desired).
   303→
   304→Stage Summary:
   305→- v0.2.0: three substantive defects fixed (one sync-protocol correctness bug that would have hit every real user on their first edit, one data-integrity race, one environment/theme issue), five features added, styling polish applied. tsc/eslint clean; all flows re-verified in browser including offline-first claim and conflict resolution paths.
   306→- Risks/next: (a) Turbopack dev memory growth — restart dev server between long edit sessions (use the subshell detach form above); (b) the stale-CSS-chunk failure mode can recur after large theme edits — a dev-server restart is the general cure; (c) suggested next features: statement PDF export, quotation reminders, data table virtualization for >500 rows, unit tests for the sync CAS contract (documented in docs/35 but not bundled in sandbox).
   307→
   308→---
   309→Task ID: 5 (QA round 2 — quotation lifecycle sync fix + statement PDF + method chips)
   310→Agent: Z.ai Code (orchestrator)
   311→Task: Re-assess, deep-QA flows not covered last round (quotation lifecycle, convert, finalize, payments, editor, reports, company, mobile), fix defects, add features.
   312→
   313→Work Log:
   314→- QA walked: quotation detail/list, mark-sent → accept → convert-to-invoice, finalize (INV/2026-27/0007), record partial payment ₹10,000, new-invoice editor end-to-end (catalog fields → live totals ₹6,372 → synced draft DRAFT-8KE3K7FT), reports GST Summary, company numbering config (present: prefixes/default GST/round-off), mobile 390px (hamburger + table scroll OK).
   315→- DEFECT (major, sync protocol): quotation status transitions did not survive sync. Client `setQuotationStatus` pushed an unflagged upsert (with items); server's `recomputeDocument` preserves only DRAFT|CONVERTED → status forced back to DRAFT server-side → pull silently reverted the client (observed: SENT badge flipped back to DRAFT after sync; server changelog 288 showed DRAFT v2 with no SENT entry ever stored).
   316→  - Client fix: transition ops now carry `status_change: true` (no items needed) → server routes to the dedicated transition branch.
   317→  - Server fix: that branch now also adopts a client-allocated official number on DRAFT→SENT (dup check + fastForwardSequence on the quotation date) so CANON §6 numbering holds across devices.
   318→  - Verified: DRAFT→SENT (server v3 SENT), →ACCEPTED (server v4 ACCEPTED, Convert button appears), →CONVERTED (banner "converted to invoice · view invoice"), all persist after pull. Note: server transition branch intentionally skips CAS (transition validation is the guard); concurrent double-SENT from two devices resolves as a rejected op (retryable) — documented behavior.
   319→- FEATURE: statement PDF export — new src/lib/pdf/statement.ts (A4, emerald header band, company+customer blocks, period, 3 summary tiles, zebra autoTable ledger with totals, offline footer, "Rs." limitation honored); "PDF" button added beside Export CSV in CustomerStatement (period-aware, company-gated); toast verified in browser.
   320→- FEATURE: MethodBadge component (src/components/app/method-badge.tsx) — icon+color chips per payment method; wired into payments list + invoice payment history rows (restructured with hover).
   321→- QA tooling note: agent-browser accessibility refs go stale after live-query re-renders (clicks landed on the wrong/no element; `find text` missed visible buttons). Reliable pattern used instead: `agent-browser eval` with `document.querySelectorAll('button')` + text match + `.click()`, and `navigator.webdriver`-safe Radix interactions via pointerdown+click. Also: Radix Select options only exist after opening the trigger.
   322→- Server data verification pattern: query Prisma scoped by the active workspace (owner2@acmetraders.in → workspace 8e284f6b…); beware `findFirst` without workspace filter — the dev DB holds an orphaned older workspace with colliding document numbers.
   323→- Final state: server INV/2026-27/0007 PARTIALLY_PAID paid=₹10,000, payments=5, total collected ₹1,46,541.50; local == server; pill Synced; console clean; `bun run lint` exit 0; tsc clean for src/.
   324→- Files: src/lib/db/repositories.ts (status_change flag), src/lib/server/sync-server.ts (transition branch number adoption), src/lib/pdf/statement.ts (new), src/components/app/method-badge.tsx (new), src/components/views/{customers-view,payments-view,invoice-detail-view}.tsx, CHANGELOG.md (0.3.0).
   325→
   326→Stage Summary:
   327→- v0.3.0: one major sync-protocol defect fixed (quotation lifecycle), statement PDF export and payment-method chips shipped; every sales workflow (quotation→accept→convert→finalize→payment) now verified end-to-end through real push/pull sync.
   328→- Risks/next: (a) server transition branch lacks CAS by design — two devices racing SENT on the same quotation yields a retryable rejected op; consider surfacing a friendlier message; (b) statement PDF does not paginate entry lists >1 page (autoTable paginates tables, but tiles/footer assume compact output — fine for current volumes); (c) candidate next work: table virtualization for 500+ rows, unit tests for sync CAS + transition contracts (docs/35), statement email/WhatsApp share hooks, Electron smoke test.
   329→
   330→---
   331→Task ID: 6 (QA round 3 — WhatsApp share, item reorder, cash-flow health, grid alignment fix)
   332→Agent: Z.ai Code (orchestrator)
   333→Task: Assess status, agent-browser QA, fix defects, add features + mandatory styling polish, verify, hand over.
   334→
   335→Work Log:
   336→- Read worklog (16 prior entries) + repo. Dev server healthy (200). agent-browser sweep of all 9 views + invoice detail + PDF preview + console: NO blocking bugs — app stable at v0.3.0 baseline. One inconsistency found: version strings stale (v0.1.0 hardcoded in app-shell ×2 / settings / onboarding / auth; package.json 0.2.1 vs changelog 0.3.0).
   337→- Checked candidate features against the codebase BEFORE building (aging report, backup/restore, invoice+quotation duplicate already exist — skipped those; good thing: avoids duplicate work).
   338→- FIX 1: created src/lib/version.ts (APP_VERSION) and wired all 5 display sites to it; package.json bumped to 0.4.0.
   339→- FIX 2 (pre-existing desktop bug caught by screenshot): editor line-items grid declared 8 columns but rows rendered 9 cells — unit sat under "Rate" header, rate under "Discount", GST select truncated off-grid. Rebuilt as explicit 9-col grid (reorder/desc/HSN/qty/unit/rate/disc/GST/remove) with matching header incl. new "Unit" label.
   340→- FEATURE A: Reminder → dropdown (Copy / Open in WhatsApp). wa.me deep link with pre-filled text; 10-digit phones normalised to 91-prefix; contact-picker fallback + footer shows target number. Verified with stubbed window.open (prefix 91 applied, 334-char text).
   341→- FEATURE B: line-item reorder in editor — desktop hover-revealed ↑/↓ around row number (boundary-disabled), mobile always-visible touch buttons; moveItem is pure local state reordering items[] which flows into parsed.inputs → save payload → PDF/sync unchanged. Verified: typed "ALPHA-ITEM" row 1 → move down → appears in row 2.
   342→- FEATURE C: Dashboard "Cash-flow health" card — collection rate (gradient bar; 65% = ₹1.47L/₹2.26L, cross-checked vs reports), avg days-to-pay per payment (69 days across 5 payments), overdue share (50%, amber warning tile when >25%).
   343→- STYLING: global zebra striping via color-mix(--muted) (light+dark verified); editor rows emerald hover tint + focus-within ring + hover-revealed delete; decimal inputs spin-buttons removed; KPI grid staggered rise-in (reduced-motion safe); tabular-nums on qty/rate inputs.
   344→- VERIFY: agent-browser desktop 1440 + mobile 390 (reorder buttons visible/tappable), light+dark screenshots, console clean, bun run lint exit 0, tsc --noEmit: 0 errors in src/ (only pre-existing examples/websocket + skills/ errors remain). Sync pill Synced throughout; no sync-layer changes this round.
   345→
   346→Stage Summary:
   347→- v0.4.0: 2 fixes (version unification, editor grid misalignment), 3 features (WhatsApp reminder share, line-item reorder, cash-flow health card), 3 styling upgrades, all verified in-browser.
   348→- Risks/next: (a) dashboard right column now has 3 cards + activity — consider tabs or collapse on short screens; (b) wa.me opens externally — in Electron it will hit the OS handler (shell flow untested); (c) next candidates: statement email/WhatsApp share hooks (pattern now exists), bulk draft actions (finalize/delete many), unit tests for sync CAS + transition contracts (docs/35), table virtualization >500 rows, friendlier message for racing SENT transitions.
   349→