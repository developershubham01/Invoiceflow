'use client'

import { AppRoot } from '@/components/app/app-root'

/**
 * InvoiceFlow — single-page application root.
 * Clean HTML5 path navigation (/dashboard, /invoices, /settings, /login).
 */
export default function Home() {
  return <AppRoot />
}
